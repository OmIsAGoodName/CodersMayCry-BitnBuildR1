version: '3.8'

services:
  vendora-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vendora-offline-orders
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - PORT=5173
    restart: unless-stopped
